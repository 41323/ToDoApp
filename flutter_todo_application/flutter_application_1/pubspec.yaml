name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  provider: ^6.0.0  # 상태 관리를 위해 추가
  sqflite: ^2.0.0+3  # 로컬 데이터베이스 사용
  table_calendar: ^3.0.0  # 달력 패키지 추가
  timezone: ^0.10.0  
  permission_handler: ^11.3.1
  shared_preferences: ^2.2.0
  flutter_local_notifications: ^18.0.1   # flutter_local_notifications의 최신 버전

flutter:
  uses-material-design: true

platforms:
  android:  # Android 플랫폼 활성화
  ios:      # iOS 플랫폼 활성화
  macos:    # macOS 플랫폼 활성화
  windows:  # Windows 플랫폼 활성화
  # linux:  # linux 플랫폼 비활성화 (아예 해당 줄을 삭제)
